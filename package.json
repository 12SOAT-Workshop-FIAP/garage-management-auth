{
  "name": "workshop-auth",
  "version": "1.0.0",
  "description": "",
  "dependencies": {
    
    "express": "^4.19.2",
    "serverless-http": "^3.2.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.0",
    "dotenv": "^16.3.1",
    "pg": "^8.11.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "jest": "^29.6.1",
    "supertest": "^6.3.3",
    "eslint": "^8.44.0",
    "cross-env": "^7.0.3"
  },
  "scripts": {
    "test": "jest --runInBand",
    "lint": "eslint . --ext .js",
    "start": "node local-server.js",
    "start:homolog": "cross-env SLS_STAGE=homolog node local-server.js",
    "start:prod": "cross-env SLS_STAGE=prod node local-server.js",
    "deploy:homolog": "npx serverless deploy --stage homolog",
    "deploy:prod": "npx serverless deploy --stage prod",
    "deploy": "npx serverless deploy --stage $npm_config_stage"
  }
}