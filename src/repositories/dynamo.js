// Dynamo repository removed — project now uses Postgres only.
// Kept file as placeholder to avoid breaking imports in older branches.
module.exports = {
  getUserById: async () => null,
  getUserByEmail: async () => null,
  createUser: async () => null,
};
